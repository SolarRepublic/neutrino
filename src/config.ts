export const GC_NEUTRINO = {
	/**
	 * Maximum time to wait for a WebSocket connection before giving up and using LCD calls as a fallback
	 */
	WS_TIMEOUT: 10e3,

	/**
	 * Average block time interval. Determines polling rate
	 */
	BLOCK_TIME: 6e3,
};
